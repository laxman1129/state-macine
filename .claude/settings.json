{
    "hooks": {
      "PreToolUse": [
        {
          "matcher": "Bash",
          "hooks": [
            {
              "type": "command",
              "command": "if echo \"$CLAUDE_CODE_GIT_PREVENT_COMMITS\" | grep -q \"true\"; then echo \"Git commits are disabled for security reasons. If you need to commit, please contact the repository administrator.\" && exit 1; fi"
            }
          ]
        }
      ],
      "PostToolUse": [
        {
          "matcher": "Bash",
          "hooks": [
            {
              "type": "command",
              "command": "if echo \"$CLAUDE_CODE_GIT_PREVENT_COMMITS\" | grep -q \"true\"; then echo \"Git commits are disabled for security reasons. If you need to commit, please contact the repository administrator.\" && exit 1; fi"
            }
          ]
        }
      ]
    },
    "permissions": {
      "deny": [
        "Bash(git commit *)",
        "Bash(git push *)",
        "Bash(git merge *)",
        "Bash(git add *)"
      ],
      "defaultMode": "dontAsk"
    }
  }